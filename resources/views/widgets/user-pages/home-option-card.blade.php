<!--option cards on home page-->
<div class="card option-cards">
    <img class="card-img-top" src="{{ asset($img) }}" alt="Card image cap">
    <div class="card-body">
        <a href="{{ route($link) }}" class="whitelink" ><h5 class="card-title">{{ $title }}</h5></a>
    </div>
</div>